# Thermalog Backend Environment Configuration Template
# Copy this file and rename to .env in the backend directory
# Replace placeholder values with your actual configuration

# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# CORS Origin (your frontend domain)
ALLOWED_ORIGIN=https://your-domain.com

# Example configurations:

# For local development with TimescaleDB container:
# DATABASE_URL=postgresql://postgres:password@timescaledb:5432/mydatabase

# For local development with external DB:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/mydatabase

# For production with cloud database:
# DATABASE_URL=postgresql://username:password@your-cloud-db:5432/thermalog?schema=public

# Additional environment variables (add as needed):
# NODE_ENV=production
# JWT_SECRET=your-jwt-secret
# API_KEY=your-api-key
# SENTRY_DSN=your-sentry-dsn